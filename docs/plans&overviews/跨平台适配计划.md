# 跨平台适配计划

> Core 当前约 30 行 Windows 特化代码。改为跨平台不新增模块，只是实现质量提升。
>
> **✅ 本方案已全部实现（Phase 2）**：`worker.py` 已使用 `psutil` 替代 `taskkill`，终端接管已用 `platform_opener()` 封装。

---

## 1. 进程树杀死

**现状**：`taskkill /F /T /PID <pid>`

**关联需求**：Phase 2 MCP 透传（`RuleWhisper联动与框架优化建议.md` 第二章）要求 Worker 终止时自动递归 kill MCP Server 子进程。改造后的 `psutil` 方案（`children(recursive=True)`）天然满足此需求。

**已改为**：`psutil`（见 `worker.py:_kill_pid_tree()`）

```python
def _kill_pid_tree(pid: int) -> None:
    """同步：用 psutil 杀掉指定 PID 及其所有子进程树。"""
    try:
        parent = psutil.Process(pid)
        for child in parent.children(recursive=True):
            try:
                child.kill()
            except psutil.NoSuchProcess:
                pass
        parent.kill()
    except psutil.NoSuchProcess:
        pass
```

**影响**：
- `worker.py` `_kill_pid_tree()` — 已使用 `psutil`
- 已消除 `taskkill`/`tasklist` 调用
- `psutil` 已加入 `requirements.txt`

---

## 2. 接管终端

**现状**：`powershell.exe -NoExit -Command` + `CREATE_NEW_CONSOLE`

**已改为**：`platform_opener()` 函数（见 `server.py`）

```python
def open_terminal(cmd: str, cwd: str):
    if sys.platform == "win32":
        subprocess.Popen(
            ["powershell.exe", "-NoExit", "-Command", cmd],
            cwd=cwd, creationflags=subprocess.CREATE_NEW_CONSOLE,
        )
    elif sys.platform == "darwin":
        script = f'tell app "Terminal" to do script "{cmd}"'
        subprocess.Popen(["osascript", "-e", script], cwd=cwd)
    else:
        try:
            subprocess.Popen(["gnome-terminal", "--", "bash", "-c", cmd], cwd=cwd)
        except FileNotFoundError:
            subprocess.Popen(["xterm", "-e", cmd], cwd=cwd)
```

---

## 3. CLI 可执行文件路径

**现状**：adapter 硬编码 `D:\node_npm\node_global\cbc.cmd`

**已改为**：Adapter 使用 `shutil.which("cbc")` 查找，配置文件可覆盖。

---

## 4. 路径处理

**现状**：混用 `\\` 和 `/`

**已改为**：全项目使用 `pathlib.Path`，`as_posix()` 用于序列化。

---

## 优先级

跨平台改造已在 **Phase 2 早期**完成，当前 `worker.py` 无 Windows 特化代码。
