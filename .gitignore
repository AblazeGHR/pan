# Python virtual environment
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo

# Node (if any)
node_modules/

# React 前端构建产物
packages/web/dist/
*.tsbuildinfo

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store

# Runtime data
data/

# Personal notes
privatenote.md
devNote.md
private-manifest.json

# Agent instructions (local only)
CODEBUDDY.md
testoutcome.md
# spike residues (may be locked by running processes)
spike-node/
spike-python/
.codebuddy/
myTODO.md
prompt.md
/.playwright-cli/
# config
config.json
.mcp.json
# opencode adapter 运行时写入的项目级 MCP 配置（spawn opencode 会话时生成）
opencode.json
# compiled from ts/app.ts — do not edit directly
packages/web/static/js/app.js
packages/web/static/js/app.js.map
packages/qq/.env

# Runtime memory DB (per-character sqlite)
memory/char_*.sqlite
memory/char_*.sqlite-shm
memory/char_*.sqlite-wal

# e2e 测试临时产物（本地调试用，不入库）
_e2e_tmp/
_pan_test.log
_pan_codex_e2e*.log

# 本机 codex 配置目录（含本地 config.toml / 凭证）
.codex/

# Secrets / 本机绑定信息防误提交（注意：scripts/kimi-mcp-probe/*.log 历史上已被追踪，
# gitignore 不影响已追踪文件，仅拦截新增）
auth_token*
*.env
*.log

# pytest temporary bases used by local adapter/worker regression runs
.pytest-tmp*/
